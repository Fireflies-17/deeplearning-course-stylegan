# 风格混合水印审计

`results/samples/stylemix/grid.png` 中可见水印样式随部分风格源进入混合结果，尤其是由带有
明显图库文字的源样本参与时。该图只用于说明纹理和污染特征能够随风格控制传播。

这组图不是独立随机样本，因此：

- 不计入 88 张独立生成样本的水印比例；
- 不用于估计水印发生率；
- 不作为某一张训练图被记忆的证据；
- 只能表述为“水印类数据污染特征被模型学习并能在风格混合结果中复现”。
